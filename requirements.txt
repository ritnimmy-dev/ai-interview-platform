# Core framework
fastapi
uvicorn

# File uploads / form handling
python-multipart

# Database + ORM
sqlalchemy
alembic

# PostgreSQL drivers (both new psycopg and older psycopg2 for compatibility)
psycopg
psycopg2-binary

# Environment variables
python-dotenv

# Data validation
pydantic[dotenv]

# HTTP requests (sometimes used by backend for API calls)
requests
